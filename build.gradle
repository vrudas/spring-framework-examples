group("io.sfe")

wrapper {
    gradleVersion = "8.6"
    distributionType = Wrapper.DistributionType.ALL
}

subprojects {
    apply plugin: 'java'

    group 'io.sfe'

    sourceCompatibility = JavaVersion.VERSION_17

    repositories {
        mavenCentral()
    }
}
